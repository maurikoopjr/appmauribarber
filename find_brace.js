const fs = require("fs"); const code = fs.readFileSync("remote_live_app.js", "utf8"); let count = 0; let lastFunc = ""; const lines = code.split("\n"); for (let i = 0; i < lines.length; i++) { const line = lines[i]; if (line.includes("function ")) { lastFunc = line; } for (const char of line) { if (char === "{") count++; else if (char === "}") count--; } } console.log("Final count:", count, "Last func:", lastFunc);
